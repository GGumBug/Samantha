[← README로 돌아가기](../README.md)

# 성장하는 값의 구간은 열려 있어야 한다

성장 시스템(아이템·강화·레벨)이 있는 게임에서 **점수·칩·배수는 상한 없이 자란다.** 그런데 그 값을 받아 등급·구간·배수를 정하는 쪽이 **손으로 적은 표**면, 표의 마지막 칸을 넘는 날 게임이 멈추거나 조용히 틀린 값을 낸다.

`switch`의 `default: throw`는 시끄러운 실패라 그나마 낫지만, **성장이 예정된 축에서는 그것도 결함이다** — 플레이어가 잘해서 도달한 상태가 크래시가 되기 때문이다.

## 규칙

1. **등급·구간·배수는 표가 아니라 식으로 정의한다.** 식에는 마지막 칸이 없다.
2. **표가 불가피하면 마지막 구간을 "이상"으로 연다** (`>= last`). 마지막 칸이 상한이 되게 두지 않는다.
3. **조회 실패에 조용한 기본값을 주지 않는다.** 0이나 1을 돌려주면 "그 축이 없는 것"과 "범위를 넘은 것"이 화면에서 같은 모양이 된다.
4. **거듭제곱·누적 곱은 `checked`로 감싼다.** 성장이 지수면 `int` 상한은 생각보다 가깝고, 넘치면 곱셈 사슬이 **음수**가 되어 약탈 방향이 뒤집힌다.

## 판별 질문

새 계산식을 넣을 때 하나만 물으면 된다:

> **이 입력이 지금의 10배가 되면 무엇이 반환되는가?**

답이 "예외" 또는 "정의되지 않음"이면 표를 식으로 바꾸거나 마지막 구간을 연다.

## 실측 감사 (2026-09-02, Double Down)

곱셈 사슬 `Σ칩 × 점 × 박 × 고배수 × 배판`의 각 항을 전수했다.

| 자리 | 형태 | 10배 입력에서 | 판정 |
|---|---|---|---|
| `GoStopState.GoMultiplier` | 거듭제곱 루프 | 정의됨 | 식이라 안전. 단 `checked` 없음 — 형제와 비대칭 |
| `LootSettler.DrawCarryMultiplierFor` | 거듭제곱 루프 + `checked` | 정의됨, 넘치면 예외 | **모범** |
| `LootSettler.BetMinForRound` | 손 표 3칸 + `default: throw` | 예외 | **의도된 게이트** — 아래 참조 |
| `GoStopJudge` 문턱 | 식 | 정의됨 | 안전 |

### 예외 — 설계자가 정하는 축은 열지 않는다

처음에 `BetMinForRound`를 결함으로 적었으나 **틀렸다.** 그 `throw`는 `RoundBeyondCurveLength_IsRejected_ForcingCurveExtensionWhenRoundLimitGrows`라는 테스트가 지키는 **의도된 게이트**다. 이름이 곧 요구사항이다: 라운드 상한을 늘리는 사람에게 판돈 곡선도 함께 늘리라고 시킨다.

축이 갈리는 지점은 **누가 값을 정하는가**다.

| | 플레이어가 키우는 축 | 설계자가 정하는 축 |
|---|---|---|
| 예 | 점수·칩·배수·고 횟수 | 판돈 곡선·라운드 상한·막 수 |
| 범위를 아는 사람 | 없음 | 설계자 |
| 표 끝에서 | **열어야 한다** — 잘해서 도달한 상태가 크래시면 안 된다 | **막아야 한다** — 외삽하면 밸런스 수치를 조용히 지어낸다 |

4라운드 판돈을 식으로 외삽하면 아무도 정하지 않은 값이 게임에 들어간다. 크래시보다 나쁘다 — 시끄러운 실패는 고치지만 조용히 지어낸 밸런스는 몇 달을 산다. 그래서 여기서는 `throw`가 답이고, **테스트 이름이 다음 사람에게 할 일을 알려 준다.**

이 구분을 놓치면 `default: throw`를 전부 결함으로 읽고 게이트까지 뜯게 된다. 판별은 규칙이 아니라 질문 하나로 한다: **이 값이 커진 것은 플레이어가 잘해서인가, 설계자가 정해서인가.**

## 왜 성장 시스템에서 유독 위험한가

성장이 없는 게임에서는 값의 범위를 설계자가 안다. 성장이 있으면 **범위를 플레이어가 정한다** — 그리고 플레이어는 항상 설계자가 상상한 것보다 멀리 간다. 그래서 이 규칙은 "방어적 프로그래밍"이 아니라 **성장 시스템의 전제 조건**이다.

## 관련 문서

- [hand-listed-roster-decay.md](hand-listed-roster-decay.md) — 손 명부가 대상이 늘어난 날 조용히 거짓이 되는 같은 계급의 병
- [golden-case-gate.md](golden-case-gate.md) — 단계 게이트 종단값 동결
- [.claude/rules/engineering-constitution.md](../.claude/rules/engineering-constitution.md) — §4 유지보수성 체크리스트
