[← README로 돌아가기](../README.md)

# Golden Case 종단 동결 게이트 — 파이프라인 종단값 손계산 동결

다단계 계산 파이프라인(채점 → 판정 → 게이트 → 정산 등)의 단계 게이트에서, 단위 테스트와 **별도로** 전 파이프라인을 관통하는 Golden Case 를 두고 **종단값을 손계산으로 동결**하는 패턴. Slice A 고스톱 정산 파이프라인(2026-07-20, 카드 18 게이트)에서 실측.

## 왜 종단값인가 (중간값 아님)

- 단위 테스트는 **부품 정확성**만 보증 — 부품 간 연결(순서, 소비 시점, 전달 형식)의 회귀는 못 잡는다
- 중간값 동결은 해당 단계까지만 감지 — **종단값 동결은 어느 축이 회귀해도 한 케이스가 감지**
- 기대값은 코드 재실행이 아니라 **손계산**으로 산출 — 구현을 기대값의 원천으로 쓰면 회귀 감지 불가 (자기 평가)

**적용 경계**: 이 결론은 **값 파이프라인**(단계마다 값을 변환하는 흐름)에 한한다. 소유권·위치 **이동**은 종점이 같으면 종단 상태가 비트 단위로 동일해 경로 오류가 종단에 흔적을 남기지 않는다 — 그쪽은 경유 경로를 단언한다([transit-path-assertion.md](transit-path-assertion.md)).

## 구성 원칙 4가지

1. **커버리지 감사 표 선행** — 케이스 선정 전에 축×판정 표(본 사례: 7축×판정)를 만들어 각 Golden Case 가 어느 축을 통과하는지 명시. 감사 표 없이 케이스를 고르면 축 누락.
2. **종단값 손계산 동결** — 파이프라인 입력(패 구성 등)부터 종단 출력(칩 정산액)까지 손계산 유도 과정을 주석/문서로 남기고 값을 동결
3. **대칭성 케이스** — 좌석 스왑 같은 대칭 변환 후 결과 대칭성 단언 (하드코딩된 좌석/플레이어 인덱스 회귀 감지)
4. **재계산 결정론** — 같은 입력 2회 계산 결과 동일 단언 (내부 상태 오염·소비성 config 이중 소비 감지)

실측: 단위 83케이스 + 종단 Golden Case 4케이스(채점 → 박 판정 → 고 게이트 통과 단언 → 고박 → 정산 종단값 동결). 중간 단계의 게이트 통과 여부도 단언에 포함해 "어디서 깨졌는지" 진단 가능하게 유지.

## 룰 모호점 확정 루프 (동결 보류 → 사용자 확정 → 기록)

Golden Case 작성 중 검증자가 **룰 자체의 모호점**을 발견하면(예: 피박 패자 피 0장 성립 여부 — Ruleset 문언 vs 전통 변형) 코드 형태로 디자인 의도를 단정하지 않는다 (헌법 Step 2). 절차:

1. **해당 경계의 동결 케이스를 의도적으로 보류** — 추측값으로 동결하면 잘못된 룰이 테스트로 고착
2. **사용자에게 옵션 제시·확정** — 각 해석의 결과 차이를 구체 예시로 제시
3. **Ruleset 변경 기록 항목 추가** — 확정된 해석을 룰 문서에 기록 (구두 확정은 휘발)
4. **확정 후 동결 케이스 추가** — 보류했던 경계를 케이스로 박제

실사례(2026-07-20): 피박 패자 피 0장 — 옵션 A(문언 유지) 확정 → Ruleset 변경 기록 + 경계 동결 케이스 추가.

## 동결의 종단은 어디인가 — 계산이 없는 전사 구간

실측(2026-09-09): 약탈 Σ칩이 **도메인 → 응용 스텝 → 표현 요약 → 화면** 4계층을 내려오는데, 도메인 아래는 전부 **옮겨 적기**다(변환도 집계도 없다).

```
LootSettler.LensChipSum → SettlementAppliedEvent.LensChipSum
  → RoundSettledStep.LensChipSum → RoundSummary.LensChipSum → 정산창 "CHIP SUM 137"
```

도메인 쪽(`LootSettlerTests`)은 두껍게 물려 있었는데 **그 아래 전사 구간은 시험이 통째로 비어 있었다.** 이유가 구조적이다 — **계산이 없으니 테스트를 붙일 이유가 안 보인다.** 그런데 바로 그래서 가장 조용히 깨진다:

- 한 칸을 떨어뜨려도 컴파일 통과(전사는 대입 한 줄이라 타입이 안 깨진다)
- 배치 통과(아무 단언도 그 칸을 안 본다)
- 화면만 `CHIP SUM 0` — **0은 예외가 아니라 그럴듯한 숫자라 눈으로도 안 걸린다**

**처방 둘**:

1. **종단 동결의 끝을 도메인 반환값이 아니라 화면에 실리는 값까지 끈다.** 위 `§왜 종단값인가` 의 논지를 계층 축으로 연장하는 것 — 중간값 대신 종단값을 무는 이유가 전사 구간에서도 그대로 성립한다.
2. **전사 이음매마다 뮤테이션으로 확인한다** — 그 칸을 0으로 바꾸거나 대입 줄을 지워 빨간불을 본다. 이번엔 두 이음매(변환기·요약)를 각각 흔들어 **각각 대응 케이스 하나씩만** 빨간불임을 확인했다.

**식별 신호**: 새 필드가 **읽는 곳마다 이름이 같고 값이 그대로**면 전사 구간이다. 그런 필드는 계산이 없어 단위 테스트 후보로 안 보이지만, 계층 수만큼 떨어뜨릴 자리가 있다.

**분류 보강**: [transit-path-assertion.md](transit-path-assertion.md) 가 "값 파이프라인 / 소유권 이동" 2분법을 든다면, **전사**는 그 사이의 셋째 성질이다 — 변환도 이동도 아니고 **복제**다.

## 게이트 위임 체크리스트

- [ ] 커버리지 감사 표(축×판정) 선행 작성
- [ ] 기대값 산출 방식이 손계산인지 확인 (구현 재실행 금지)
- [ ] 대칭성·재계산 결정론 케이스 포함
- [ ] 룰 모호점 발견 시 동결 보류 → 사용자 확정 → 기록 → 동결 순서 준수

## 관련 문서

- [.claude/rules/evaluation.md](../.claude/rules/evaluation.md) — 평가 주도 검증·작성자/검증자 분리
- [.claude/rules/engineering-constitution.md](../.claude/rules/engineering-constitution.md) — Step 2 "코드 형태로 디자인 의도 단정 금지"
- [determinism-hooks.md](determinism-hooks.md) — 결정론 RNG 보존
- [transit-path-assertion.md](transit-path-assertion.md) — 소유권·위치 이동의 경유 경로 단언(적용 영역이 갈리는 짝)
