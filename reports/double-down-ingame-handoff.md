[← README로 돌아가기](../README.md)

# Double Down 인게임 핸드오프

2026-08-29 갱신. 다른 PC에서 이어가기 위한 인수인계. 대상 저장소는 **Double Down** — 현재 작업 브랜치는 **`feature/floor-choice`**(`b9547cb`까지, 원격 푸시 완료·워킹트리 clean).

> **검증 루프가 코디네이터 안에서 닫힌다**: 에디터만 닫으면 macOS 에서도 배치 모드가 돈다(EditMode **790** + PlayMode **54**). 사용자 왕복 없이 수정→증명 사이클이 가능하고, "이 테스트가 정말 그 버그를 잡는가"까지 **뮤테이션 1줄 토글**로 기계 증명한다 — §4.
>
> MatchView `IUIService` 마이그레이션과 계기판 배선은 **완료**되어 이 문서의 주제가 아니다(§7 이력). 살아 있는 작업은 §3이다.

## 1. 확정 결정 (재논의 금지)

### UI 구조

| 결정 | 내용 |
|------|------|
| UIManager 실체 | GameCore의 `IUIService`/`UIService`. **새 관리자 클래스 신설 금지**(SSOT 위반) |
| UI 로드 경로 | Addressables, 주소 = `typeof(T).Name` |
| 화면 스펙 SSOT | Notion 「인게임 화면 구성 (확정)」 — **유추 금지, 원문을 먼저 연다**(헌법 §0-1) |

### 룰 (이번 세션에서 확정 — GDD §2-1-1에 반영됨)

| 항목 | 확정 내용 | 근거 |
|---|---|---|
| 고/스톱 제시 | **문턱 점수 하나**(기준선 7점) + 재선언 가드. 복수 라인·상대 우세는 **후반 조건부 페널티로 이월** | 초반에 강화가 없어 방어할 인플레도 없었다 |
| 빈손 제시 | 손패가 비면 **묻지 않고 스톱 확정**. 좌석 무관(AI 포함) | 고는 남은 손패에 거는 베팅 — 빈손 고는 이득 0에 고박 리스크만 |
| 소진 | 스톱 없이 패가 다 떨어지면 **항상 나가리** | 시뮬레이션 실측: 소진 승자 849판 **전부**가 이미 제시를 받았고 75%는 마지막 턴에도 받고 고를 택했다 — "물어보지도 못하고 끝나는" 판은 없었다 |
| 나가리 배판 | 다음 판 약탈액 **×2**, 연속이면 누적 곱. 배수는 **패자 칩 상한·bet_min 하한보다 앞에서** 곱한다 | 뒤에서 곱하면 가진 것보다 많이 뺏어 칩 총량 보존이 깨진다 |
| 상대 스톱 체감 | **룰은 바꾸지 않는다.** 연출로 해결(GDD 미정 목록 등록) | 먼저 문턱을 찍은 쪽이 스톱하는 것이 옳은 플레이, 좌석마다 다른 룰은 SSOT를 흐린다 |

## 2. ⚠️ 새 PC에서 첫 3분

1. **양쪽 저장소 pull** — Double Down은 **`feature/floor-choice`** 를 checkout 한다(`master` 로 시작하면 이 브랜치의 택1 팝업 커밋이 안 보인다), Samantha `master`
2. **Unity 열고 컴파일 확인** — 에러 0건
3. **`tools/ddtest.sh` 경로 3줄 수정** — `SRC`·`DST`·`UNITY`. 그러면 779건이 13초에 돈다(§4)
4. **재생 검증 4종** — 이번 세션 수정분이 살아 있는지:
   - 다음 판으로 넘어갔을 때 더미·상대 손패가 **전부 뒷면**인가
   - 쪽/따닥/싹쓸이/뻑해소 시 **상대 획득패에서 피 한 장이 날아오는가**
   - 카드가 UI 패널에 **물리지 않는가** (콘솔에 `판을 N%로 축소해 창에 맞춘다` 정보 로그)
   - 마지막 패를 눌렀을 때 **치기 → 상대 턴 → 상대 획득 → 스톱 → 종료**가 순서대로 보이는가
5. **택1 팝업 확인 (2026-08-29 신규)** — 바닥 짝이 2장 이상인 국면에서:
   - 후보 카드가 **커진 채 유지**되고 그 아래로 딤이 페이드 인 하는가 (커졌다 곧 작아지면 §5 수명 함정 재발)
   - 딤이 떠 있어도 **후보 카드가 클릭되는가** (딤은 입력을 먹지 않는다)
   - 고르면 딤이 사라지고 획득 연출로 이어지는가

## 3. 지금 하던 일 — 인게임 룰·연출 다듬기

MatchView 배선은 끝났다. 작업 축이 **룰 정합 → 연출 품질 → 레이아웃 정합**으로 옮겨갔다.

**2026-08-29 시점**: 플로팅 텍스트(획득 +N · 약탈 · 뻑 · 족보 완성)와 택1 팝업(후보 실물 확대 + 딤)이 끝났다. 둘 다 §7 에 이력이 있다. 가장 가까운 미완은 **택1 Part B2**(겹친 후보 분리) 다.

### 미결정 (사용자 판단 대기)

| 항목 | 내용 |
|---|---|
| 후반 페널티 부착 축 | 복수 라인·상대 우세를 무엇에 매다나 — 막 / 선수 등급 / 누적 고 횟수. **선수 등급 유력**(Balatro 보스 블라인드 문법) |
| 마지막 턴 약탈 무효 범위 | 전통은 쪽·따닥·싹쓸이·뻑해소 **4종 전부** 무효인데 현재는 **쪽만**. 룰셋 문서가 그렇게 정한 의도된 편차 — 넓힐지 결정 필요 |
| 폭탄·자뻑 | 상대 피를 가져오는 전통 룰 6종 중 미채용 2종. 흔들기가 미채용이라 폭탄도 보류 상태 |

### 다음 후보

- **⭐ 택1 Part B2 (가장 가까움)** — 같은 자리에 겹친 후보 2장이 **전부 `천장+2` 동일 order** 라 상호 앞뒤가 Z(전부 0)로 갈리지 않는다. 후보 간 분리(자리 오프셋 또는 order 내 index 가산)가 필요하다. Jarvis 가 Part A 에서 보고만 하고 남긴 항목
- **택1 연출 수치 조정** — 지연 0.12s · 페이드 0.22s(`FloorChoicePopupView` 파일 상수) / 확대 1.25배 · 리프트(`TurnBeatBudget` 인스펙터, 즉시 반영) / 딤 0.55(프리팹 `Image_Dim`)
- **`FloorChoicePopupView` 개명** — 실제 역할은 "막" 한 장인데 사본 시절 이름이 남았다. 타입명이 곧 Addressable 주소(`typeof(T).Name`)라 프리팹 이름 · 주소 등록(`UIAddressableRegistrar`) 동반 필요
- **턴 전환 연출** — 200ms 쉼이 그 자리다. "상대 차례" 표시, 상대 점수가 문턱에 가까워지는 긴장 고지, 스톱 선언의 타격감
- **설정 화면 ↔ 빠른 전투** — `TurnBeatBudget.SetFastBattle(bool)` 하나만 부르면 된다
- **등급·막·상대 이름 데이터화** — 현재 프리팹 문구 고정

### 보류 결정 — GitHub Actions (2026-08-24, 재검토 금지)

**하지 않는다.** "아직 안 했다"가 아니라 **비용이 이익을 넘어서 접은 것**이다.

- **새로 검증되는 것이 없다** — Actions 가 돌릴 명령이 `tools/ddtest.sh` 와 같다(§4). 검증 내용이 동일하고 그쪽은 13초에 끝난다.
- **이익이 좁다** — 값을 하는 자리는 "코디네이터가 없는 사이 쌓인 커밋"뿐이다. 낡은 테스트 13건이 나흘 잠복한 것은 CI 부재가 아니라 **배치 실행 부재** 때문이었고, 그건 해결됐다.
- **비용이 실질적이다** — Personal 라이선스는 CI 활성화가 까다롭고 동시 사용 제약이 있다(데모 `unity-cli-ci-demo` 가 Pro 시리얼을 전제한 이유). 계정 자격증명을 Secrets 에 올려야 하고, 에디터 매번 설치로 회당 10~20분이라 캐시가 필수인데 그 캐시가 또 유지보수 대상이 된다.
- **CI 의 본래 값은 다중 커밋자 환경에서 난다** — 1인 개발 + 작업 PC 2대 구조에서는 "작업 단위마다 13초 돌리는 습관"이 같은 효과를 더 싸게 낸다.

**다시 볼 신호** — 셋 중 하나가 생기면 그때 재검토한다: ① 협업자 합류 ② 다른 PC 작업이 잦아져 코디네이터 없는 커밋이 누적 ③ **릴리스 빌드 자동화** 필요(목적이 테스트가 아니라 산출물이 되면 값어치 계산이 달라진다).

## 4. 검증 도구

배치 실행 스크립트는 **[`tools/ddtest.sh`](../tools/ddtest.sh)로 저장소에 커밋했다** — scratchpad는 세션과 함께 사라지므로(같은 PC에서도 사라졌다) 도구 자체를 남긴다. 경로 상수(`SRC`·`DST`·`UNITY`)는 이 PC 기준이라 **새 PC에서 그 세 줄만 고치면 된다**.

아래 함정 목록은 스크립트 주석에도 박혀 있다. 스크립트를 잃었을 때 재작성 비용의 대부분이 이 함정들이다.

### ⭐ Unity 배치 모드 실행 (주 도구 — `tools/ddtest.sh`)

**EditMode 779건이 13초.** `Presentation.Tests` 192건을 포함해 **모든 테스트를 실제로 실행**한다. Personal 라이선스로 동작하며 `unity` CLI 설치가 필요 없다.

```bash
bash ddtest.sh                    # 전량 779건
bash ddtest.sh --filter DoubleDown.Presentation.Tests    # 부분
bash ddtest.sh --full-sync        # 사본 재생성 (Library 캐시 버림, 수 분)
```

**구조** — 원본이 아니라 **사본**(`scratchpad/dd-batch`)에서 돈다:

```
SRC=C:/Unity_Projects/Double-Down
DST=<scratchpad>/dd-batch
UNITY="/c/Program Files/Unity/Hub/Editor/6000.3.19f1/Editor/Unity.exe"

# 1) Assets·Packages·ProjectSettings 만 증분 동기화 (Library 는 제외)
MSYS_NO_PATHCONV=1 robocopy "$(cygpath -w "$SRC/$d")" "$(cygpath -w "$DST/$d")" \
  /MIR /NFL /NDL /NJH /NJS /NP /R:1 /W:1

# 2) 실행 — -quit 을 주지 않는다
"$UNITY" -batchmode -nographics -projectPath "$DST" \
  -runTests -testPlatform EditMode \
  -testResults "$DST/results-editmode.xml" -logFile "$DST/batch.log"

# 3) results-editmode.xml 을 파싱해 어셈블리별 집계 + 실패 상세 출력
```

**재작성 시 반드시 지킬 것 — 다섯 함정을 전부 밟았다**

1. **`-quit` 금지.** `-runTests` 는 끝나면 스스로 종료한다. `-quit` 을 함께 주면 임포트 직후 종료해 **테스트에 도달하지 못하고 그때도 종료 코드 0** 을 낸다(성공으로 오인).
2. **사본에서 돌려야 한다.** Editor 가 원본을 `Temp/UnityLockfile` 로 잠그므로 같은 폴더를 배치 모드가 못 연다. 사본이면 작업 중 Editor 를 닫지 않아도 된다.
3. **`Library` 를 동기화에서 제외한다.** 사본이 쌓아 둔 임포트 캐시(약 2.2G)가 재임포트를 건너뛰게 하는 **자산**이다. 이것 하나가 5~10분을 13초로 줄인다.
4. **`MSYS_NO_PATHCONV=1` 필수.** Git Bash 가 `/MIR` 을 경로로 오인해 `C:/Program Files/Git/MIR` 로 바꿔 `robocopy 16` 이 난다. 슬래시 인자를 쓰는 네이티브 명령 전부에 해당한다.
5. **출력 인코딩을 UTF-8 로 고정한다.** 콘솔 기본이 cp949 라 한글·em dash 가 `UnicodeEncodeError` 를 낸다. `PYTHONIOENCODING=utf-8` + `sys.stdout.reconfigure(encoding='utf-8')`.

**라이선스는 문제가 아니다** — 초기 로그의 `Code 10 while verifying Licensing Client signature` 는 첫 핸드셰이크 실패일 뿐이고 재연결 후 `Successfully resolved entitlement details` 가 찍힌다. **첫 에러에서 읽기를 멈추면 "라이선스가 막는다"로 오판한다**(실제로 그렇게 오판했다).

### macOS 직접 실행 (2026-08-29 실증 — 사본 없이)

`tools/ddtest.sh` 는 Windows 전용(robocopy·cygpath)이다. **macOS 에서는 사본 없이 원본을 직접 돌렸고 잘 됐다** — 대신 **Unity 에디터를 반드시 닫아야 한다**(원본 락).

```bash
UNITY=/Applications/Unity/Hub/Editor/6000.3.19f1/Unity.app/Contents/MacOS/Unity
"$UNITY" -batchmode -nographics -projectPath "/Users/minki/Unity_Projects/Double Down" \
  -runTests -testPlatform EditMode \
  -testResults "$SP/em.xml" -logFile "$SP/em.log"
```

실측: EditMode 790건 · PlayMode 54건. `-testPlatform` 을 바꿔 두 번 돌린다.

**결과 판정** — `head -3 <xml>` 의 루트 속성이 전부다:

```bash
head -3 "$SP/em.xml" | tr ' ' '\n' | grep -E "^(result|total|passed|failed|skipped)="
```

**`skipped` 를 반드시 함께 볼 것.** `MatchRestartTests` 는 프로덕션 자산이 없으면 `Assert.Ignore` 로 빠지도록 설계돼 있어, 무시돼도 `failed=0` 이라 총계만 보면 "통과"로 읽힌다.

**`-testFilter` 는 글롭이 아니라 .NET 정규식이다.** `Regex.IsMatch` 부분 일치라 와일드카드가 불필요하고, `*Foo*` 를 주면 `Quantifier {x,y} following nothing` 으로 파싱이 죽는다. 그러면 **결과 XML 이 안 생기고 프로세스는 조용히 종료**한다 — `-quit` 함정과 같은 계급의 무실행이다. 완료 판정은 프로세스 종료가 아니라 **XML 존재**로 한다.

```bash
-testFilter "FloorChoiceLiftLifetime"      # ✓ 부분 일치
-testFilter "*FloorChoiceLiftLifetime*"    # ✗ 정규식 파싱 실패 → 무실행
```

**행 판정 3지표** — 시동 직후 굳는 일회성 증상을 한 번 겪었다. 셋이 동시에 성립해야 "죽었다"로 본다: CPU ~0% + 로그 크기 정지 + **완주본 로그 크기 대비 미달**(정상 완주 ~96KB). 처방은 `pkill -9 -f "batchmode.*runTests"` 후 `Temp/UnityLockfile` 제거 뒤 재시도.

### 뮤테이션 검증 — "이 테스트가 정말 그 버그를 잡는가"

초록불만으로는 **"통과한다"와 "무언가를 검증한다"가 구별되지 않는다**(관측 창구가 잘못 배선돼 항상 참인 단언도 초록불이다). 방금 고친 그 변이 하나에 대해서만 수동 뮤테이션을 돌리면 거의 공짜로 증명된다:

1. 수정 상태로 실행 → 전부 초록
2. **버그를 정확히 한 줄 되돌림** → 예상한 케이스만 빨간불인지 확인
3. 즉시 복원 (`trap restore EXIT` 로 스크립트가 죽어도 원복되게)

2026-08-29 택1 수명 버그에서 4종 중 정확히 2종(`Survives*`)만 실패해, 테스트·관측 창구·수정이 한 번에 증명됐다.

### 리플렉션 테스트 러너 (보조)

배치 모드가 되므로 이제 보조 수단이다. Unity 없이 도메인만 빠르게 볼 때 쓴다 — `Temp/bin/Debug/*.Tests.dll` 을 리플렉션으로 열어 NUnit 테스트를 직접 호출한다(573건).

- 대상은 UnityEngine 비의존 3종(`Domain`·`Application`·`Infrastructure`)뿐. `Presentation`·`Integration` 은 실행 불가 — **그 구멍이 배치 모드 도입의 이유였다**(낡은 테스트 13건이 나흘간 잠복했다).
- **핵심 함정**: 프로젝트 어셈블리를 `<Reference>` 로 참조하면 빌드가 사본을 러너 폴더에 복사하고 `Assembly.LoadFrom` 이 그 **낡은 사본**을 먼저 해석한다. `<Private>false</Private>` + `AssemblyResolve` 훅으로 `Temp/bin/Debug` 에서 찾게 할 것.
- 클래스별 실행 건수를 찍어 두면 "그 테스트가 정말 돌았나"를 증명할 수 있다(통과는 출력되지 않으므로).

### 시뮬레이션 프로브

`MatchFlow` + `HeuristicAiPolicy`로 수천 판을 돌리며 이벤트 스트림에서 불변식을 전수 검사한다. `IEventSink` 구현 하나면 되고, `Pcg32RandomFactory`(Infrastructure)를 쓴다.

이번 세션 실적:
- **마지막 패 역전 분포** — 소진 승리 1,133판 중 32.4%가 "마지막 장에서 문턱 첫 돌파"
- **특수상황 미배선 발견** — 2,000시드에서 발생 0건 → 원인 확정
- **약탈 존 이동 불변식** — 4,294건 전수 위반 0

## 5. 함정 (이미 밟은 것 — 반복 금지)

### 2026-08-29 세션에서 새로 밟은 것

- **⭐ 수명이 다른 두 상태를 같은 해제 지점에 묶지 말 것**: `TurnBeatDirector.CancelAll` 의 계약은 "날고 있는 모션을 끊는다"(스텝 수명)인데 거기에 **택1 들어올림 해제**(국면 수명)를 얹었다. `StepPlayer` 가 배치 꼬리마다 `CancelAll` 을 부르고 택1 대기로 끝나는 배치는 `EndsWithFloorChoicePending` 이 인정하는 정상 모양이라, **사람이 고르기 전에 반드시 그 목을 지나** 후보가 커졌다 곧 꺼졌다. 짧은 수명이 긴 수명을 자른다. 컴파일·grep·기존 840종 어디에도 안 걸리고 **시간축 시나리오에서만** 드러난다.
- **호출처 탐색을 한 파일 안에서 닫지 말 것**: 위 버그의 1차 진단은 `TurnBeatDirector` **안의** `CancelAll` 호출(`CaptureBeforeRender`)만 찾아 절반만 맞았다. 진짜 목은 파일 **밖**(`StepPlayer`)에 있었고, 1차 진단대로만 고쳤으면 증상이 남았다. 검증자(테스트 작성 위임)가 이것을 잡아냈다 — 위임 프롬프트에 **"내 기대 시나리오도 틀릴 수 있다"** 를 넣은 것이 값을 했다.
- **`Editor.log` 의 `error CS` 는 낡은 잔재일 수 있다**: 30분 전에 고쳐진 코드의 에러가 그대로 남아 있었다. 판정 순서는 ① **DLL mtime**(`Library/ScriptAssemblies/*.dll` vs 소스) ② 컴파일 실패로 DLL 이 안 갱신됐다면 **에러의 파일 분포**. Unity 는 어셈블리를 통째로 컴파일하므로 "에러가 파일 A 하나뿐"이면 **같은 어셈블리의 나머지 파일은 전부 통과**라는 뜻이다(이 추론으로 Part A 4파일의 통과를 DLL 없이 판정했다).
- **`Screen Space - Overlay` 는 월드 스프라이트를 항상 덮는다 → 연쇄 결정을 만든다**: "딤이 uGUI 여야 한다 → 후보 사본도 uGUI 여야 한다 → `CardView` 의 기울임·워블·패럴랙스를 두 벌로 이식해야 한다"는 사슬 전체가 이 한 줄에서 나왔다. 캔버스를 **Screen Space - Camera** 로 내리면 연쇄가 통째로 사라지고 **실물 카드를 그대로 승격**할 수 있다(사본 0, 반응 이식 0, 입력 신규 코드 0). 기존 §5 "소팅으로 못 이긴다" 항목의 반대편 처방이다.
- **구현 시작 전에 "이미 있는 축"을 먼저 grep 할 것**: 택1 팝업을 사본 방식으로 절반 만든 뒤에야 `CardView.SetChoiceCandidate(bool)` + `TurnBeatDirector.ApplyChoiceLift` 가 **이미 후보를 들어올리고 있었다**는 것을 발견했다. 사본 사슬(스프라이트 주입·포인터 핸들러·커서 라우팅·풀 수명·후보 교체)이 통째로 불필요해졌다. 미커밋 상태라 전환 비용이 쌌다.
- **위임 프롬프트에 "찾을 거리"를 남기면 25턴이 탐색으로 소진된다**: 세 번 연속 편집 직전 절단됐다. 원인은 프롬프트 길이가 아니라 *"~를 grep 해 같은 계급으로 맞춰라"* 같은 **탐색 지시**였다. 결정을 내려주는 것과 **위치를 알려주는 것**은 별개의 비용이다. 코디네이터가 grep 6번 하는 비용이 에이전트가 30번 하는 비용보다 훨씬 싸다. 처방: 프롬프트 첫 줄에 **"구조 파악용 grep 금지 — 필요한 사실을 아래 전부 적었다"** + 정확한 파일·줄번호·API 형태를 박아 넣기.
- **절단 재개 시 상태 판별 3지표**: `git status --short` + **파일 mtime** + 산출물 grep. 특히 병행 작업(사용자가 Unity 로 프리팹 저작 중)이 있으면 **mtime 순서가 저자를 가른다** — 이번에 `MatchView.prefab`(13:06)이 에이전트 산출물(13:29~)보다 23분 빨라 사용자 작업임을 판정했다.

### 이전 세션에서 밟은 것

- **선택 인자가 미배선을 숨긴다**: `ISpecialSituationJudge`가 `= null` 선택 인자라 실게임 진입점이 안 넘겨도 컴파일이 통과했고, 판정기를 직접 꽂는 단위 테스트 60여 건은 전부 녹색이었다. **쪽·따닥·싹쓸이가 실게임에서 한 번도 발생하지 않은 채 잠복**했다. 새 축·판정기는 **필수 인자**로 넣어 컴파일러가 생산 지점 명부를 만들게 할 것.
- **`.csproj` 거짓 녹색**: 신규 `.cs`는 Unity 재임포트 전까지 `Compile Include`에 없어, 그 파일을 **아예 컴파일하지 않은 채** 0 오류로 통과한다. 신규 파일을 아무도 참조하지 않으면 영영 안 드러난다. 빌드 전에 `grep -F "<경로>" <csproj>`로 포함 여부를 확인할 것.
- **겹침과 간격 0은 다르다**: "애니메이션이 동시 진행되는 느낌"을 겹침으로 읽고 두 번 고쳤으나 실측은 겹침 0건 + 간격 0.2ms였다. 처방이 정반대(순서 강제 vs 시간 생성)다. **시간축 버그는 밀리초를 찍기 전에는 갈리지 않는다.**
- **없는 로그를 "그런 일이 없었다"로 읽지 말 것**: 상대 선언이 로그에 안 남아 "선언 없이 끝났다"고 오독했다. 실제로는 상대가 스톱을 불렀다.
- **`Screen Space - Overlay`는 소팅으로 못 이긴다**: 월드 스프라이트가 UI 뒤로 가는 문제를 소팅 레이어로 풀려다 오진했다. 실제 요구는 "위에 그리기"가 아니라 "**벗어나지 않기**"였다.
- **Mono `string.Format` 의 마지막 자리표시자**: 닫는 이스케이프 `}}` 에 인접한 자리표시자의 서식 지정자를 **리터럴로 흘린다**(`{1:F9}` → `"F9"`). `{0:F9}` 는 정상이라 서식 종류를 의심하게 되는데 원인은 **위치**다. JSON 을 조립할 때는 `string.Format` 대신 `ToString("F9", InvariantCulture)` + 문자열 연결을 쓴다. 이 진단에 두 번 실패했고, **실제 문자열을 로그로 찍은 한 줄이 끝냈다**.
- **초록불이 검증을 뜻하지 않는다**: `OpponentHand_MirrorsPlayerFan_WithFlippedSign` 은 한 번도 실패한 적 없으면서 회전·아치가 0이라 `0 == -0` 을 비교하고 있었다. 설정값을 끄면 그 값을 재던 테스트가 **공허하게 통과**한다 — 값을 끌 때는 그것을 검증하던 테스트가 무의미해지지 않았는지 함께 본다.
- **설정 기본값을 동결하는 테스트는 소리 없이 낡는다**: `ScriptableObject.CreateInstance<T>()` 를 쓰는 테스트는 `.asset` 이 아니라 **C# 필드 기본값**을 잰다. 자산과 코드 기본값을 함께 바꾸면 이 테스트만 남겨진다(획득패 배율 0.5 → 0.53 에서 2건이 그렇게 깨졌다).
- **넓은 가드를 좁힐 때는 그 가드가 막던 실제 문제를 먼저 찾는다**: `StepPlayer` 의 폴드 가드가 `RoundEnded` 까지 막아 마지막 수 연출을 버리고 있었다. 가드만 좁히면 그 가드가 막던 **권위 대조 오판**("권위에 없는 카드" — 라운드 밖 권위는 빈 목록이다)이 되살아난다. 넓은 가드를 **좁은 면제**로 바꿔야 했고(대조 생략 + 끝 렌더 생략), 그 선례가 이미 `PlaySplitAtDealAsync` 에 있었다. **가드에 이유가 주석으로 적혀 있어서 발견했다** — 없었으면 옛 버그를 되살렸을 자리다.

### 기존

- **생명주기 승격**: `MatchView`·`GoStopView`는 DDOL이라 **판보다 오래 산다**. 재시작 잔재 0은 파괴가 아니라 **상태 초기화**로 얻는다.
- **첫 진입 순서**: 씬은 **판을 먼저 세우고 UI를 나중에** 세운다. `BuildRuntime`의 `_view?.Bind(...)` 류 널 가드는 첫 판에서 조용히 지나간다 — `PrepareUiAsync`에서 보정 밀기를 반드시 함께 걸 것(`GoStopView.Bind`·판 창 정렬 둘 다 이 함정을 밟았다).
- **라운드 스코프 상태의 실제 수명**: 카드 인스턴스 id는 판마다 0부터 재사용된다. "라운드 스코프"라고 주석에 적힌 상태가 실제로 `Clear()`되는지 확인할 것(`_revealedDefinitions`가 안 씻겨 뒷면 카드가 앞면으로 샜다).
- **`ReleaseAll` 금지**: 전 뷰 파기라 로딩 커튼까지 죽인다. 씬 이탈은 `Close<T>()`.
- **stale `.csproj`**: asmdef 수정 후 Unity 재import 전까지 옛 참조다. CS0234는 코드가 아니라 이걸 의심하라.

## 6. 도달한 구조

```
IUIService (= UIManager)
 ├─ MatchView    계기판 — Render · UpdateScoreLines · ResetForNewMatch
 │   ├─ PlayWindow          판 가로 창 rect — 보드 정렬·축소의 SSOT
 │   ├─ EventBannerMotion   사건 채널 (IStepMotion) — 선언 배너
 │   ├─ HudRoundSummary     라운드 요약
 │   └─ HudRejectionFeedback 거부 흔들기
 └─ GoStopView   고/스톱 모달 + 키 힌트

StepPlayer (연출 재생)
 ├─ 스텝마다 WhenAll(자기 모션, 최소 박자)
 ├─ 폴드 가드: 분배 경계(RoundStarted)만 막는다 — 종료 스텝은 존 무변경이라 통과
 │   라운드가 닫힌 배치는 권위 대조·끝 렌더를 면제(빈 목록이 화면을 지우는 것을 막는다)
 └─ 턴 경계: NotifyTurnBoundary → 렌더 → 시각 정착 펜스 → 턴 전환 쉼
      └─ BoardView.WaitForVisualQuiescenceAsync
           └─ CardView.TryDescribeUnsettled  4채널(지정 이동·뜸·뒤집기·추종)

BoardLayout (배치)
 └─ 모든 X가 PlayAreaHalfWidth 대칭에서 파생 — 실제 크기는 PlayWindow가 정한다
      루트 위치 = 창 중심, 루트 스케일 = min(1, 창 반폭 / 밴드 반폭)
```

## 7. 커밋 이력

### 룰·연출 (2026-08-18~19)

| 커밋 | 내용 |
|---|---|
| `25b4849`·`f22e6ce`·`ff3e1f1`·`8dfc3d3`·`6d2deaa` | 분해 칸(칩 × 배수) — 스냅샷 축 → 렌더 → 배수 통합 → 프리팹 배선 |
| `96cb8cc` | 고/스톱 제시를 전통 맞고의 문턱 하나로 |
| `d3205ab` | 첫 진입에서 고/스톱 화면에 게이트를 꽂는다 |
| `178b63e` → `96cd456` → `6ae2e25` | 소진 판정 3회 왕복 — 최종은 **항상 나가리** |
| `6cb633c` | 나가리 다음 판 배판 |
| `ca6a5f9` | 판 경계에서 공개 맵 초기화 — 뒷면 카드가 앞면으로 새던 문제 |
| `5278a30` | 손패가 빈 제시는 묻지 않고 스톱 확정 |
| `021d99c` | 턴 경계 시각 정착 펜스 |
| `1eae9b6` | 턴 전환 쉼(200ms) |
| `d35d14f` | 재생 속도를 기본(0.7)·빠른 전투(1) 두 축으로 |
| `86af97c` | 특수상황 판정기 실게임 기본 배선 |
| `646826f` | 판을 UI 가로 창에 맞춤(정렬 + 축소) |

### 배치·레이아웃 (2026-08-20~24)

| 커밋 | 내용 |
|---|---|
| `9512e78` | 카드 뷰를 구역별 루트로 묶는다 — 하이어라키에 판 상태 반영, 루트는 항등 불변식 |
| `0d6e089` | 카드 재배치 — 바닥·더미 0.8 / 획득패 0.53, 손패 아치·부채 **0으로 끔** |
| `753c262` | 양측 손패를 구역 오프셋으로 판 바깥 0.7 밀기 |
| `b8c9397` | 바닥 레일 고정 간격 모드(`FixedPitch`) — 균등 분할의 넘침 보장을 부트 검산으로 대체 |
| `f90ccbb` | 바닥패 배치 — 같은 달 부채 회전을 **계단식 적층**으로(X 확산 + Y 들림), 레일 배수 1.2293 |
| `69f974d` | 배치 실행으로 드러난 낡은 배치 기대값 갱신 (13건 → 0건) |
| `c6d8552` | 라운드가 닫히는 배치도 마지막 수를 연출한다 — 폴드 가드를 분배 경계로 좁히고 대조·끝 렌더 면제 |

Samantha 쪽: `cf12717` 배치 실행 도구 커밋 + 핸드오프 반영 · `09fc266` GitHub Actions 보류 결정

### 플로팅 텍스트 (2026-08-25~28, master)

| 커밋 | 내용 |
|---|---|
| `0d8e0f9` | 카드 획득 플로팅 텍스트 — 발라트로식 +N. 뷰·스포너·`IStepMotion` 세 번째 채널(0초 계약) |
| `deeb7f4` | `FloatingTextConfig.asset` 보강 — 직전 커밋 누락분. 없으면 `Resources.Load` 가 null 이라 **연출이 조용히 꺼진다** |
| `093b9c7` | 체육관 발사 4종 — 낱장·따닥 동시·연타·색 견본 |
| `0a5abed` | 약탈 금색 · 뻑 적색 · 피 값 기준 크기 변주 |
| `b7d45d2` | 족보 완성 해금 — `ScoreUpdatedStep.CompletedLines` 는 **상태 전량**이라 그대로 띄우면 매 턴 재등장. 변환기가 `NewlyCompletedLines` **델타를 선언**하고 채널은 읽기만 한다 |

### 턴 재구성 (2026-08-28~29, master)

`3f66da2` 턴을 9페이즈로 재구성하고 모든 패가 바닥을 경유 · `4385a47` 권위 스냅샷의 죽은 마스킹 예외 제거 · `933b50a` 연출도 모든 패가 바닥을 경유하게 정합

### 택1 팝업 (2026-08-29, `feature/floor-choice` — **원격 푸시 완료**)

원안(사본 UI Image)을 **실물 승격**으로 뒤집었다. 근거: 사본이면 `CardView` 의 반응 어휘(손끝 기울임·아이들 워블·속도 tilt·셰이더 패럴랙스 + 반감기 손잡이 10여 개)를 **두 벌 유지**해야 하고 어긋나도 시각으로만 드러난다.

| 커밋 | 내용 |
|---|---|
| `2889892` | 후보를 실물 그대로 확대(1.25배)해 딤 위로. 정렬 파생 `OverlaySortingCeiling` 하나에서 **딤+1 · 후보+2 · 수치+3** (수치는 기존 +1 에서 조정). `MaxSortingOrder(32767)` 난간 포함 |
| `322ac08` | 딤 — 막 뷰(`IUIView`, 알파 전환, `blocksRaycasts=false`) · 감독 트리거 · `MatchRuntime.SetChoiceDim` 통과 · `MainScene` 의 `GetAsync` 프리로드(**`OpenAsync` 금지** — 택1 국면 전에 전면 딤이 입력을 삼킨다) |
| `2e7c8a5` | 들어올림 수명 계약 4종 동결 (PlayMode) — 뮤테이션 검증 완료 |
| `b9547cb` | 배경 테두리 추가 (사용자 직접) |

검증: EditMode 790/790 · PlayMode 54/54 · 뮤테이션 2/2.

### 이전 (MatchView 마이그레이션 — 완료)

`223e569` 좌 컬럼 재구성 · `fcee59f` Part A · `2d5210f` Part B · `3d2410d` 슬롯 배지·라운드 요약 이관 · `5d155f2` 배너 채널 · `b418d04` 거부 피드백 · `2ca0540` 옛 HUD 제거(−8,572줄) · `2ebaf84` 필요 점수 축 · `ffb57e1` 좌석 점수 칸 · `0fdf991` GoStopView 주소 등록

## 8. 유실 기록

`reports/balatro-animation-improvement-plan.md`(카드 29 juice 개선 계획, 2026-07-29)가 커밋되지 않은 채 사라졌다. git에 없어 복구 불가. 요지: 발라트로의 매력은 이동이 아니라 **반응 채널**(juice 펄스·아이들 워블·포커스 반응·점수 롤링·화면 흔들림)에서 나오고, 현 구현은 이동 채널만 있다. 카드 29 재개 시점에 재분석할 것.
